/**
 * Body diagram viewBoxes — path geometry now lives in `body-highlighter/`
 * (vendored from react-native-body-highlighter, MIT).
 */
export {
  BODY_HIGHLIGHTER_VIEWBOX_BACK,
  BODY_HIGHLIGHTER_VIEWBOX_FRONT,
  bodyHighlighterViewBox,
} from "./body-highlighter"
